<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.peterknut.maintainservice">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PersonalInfromationActivity"
            android:label="@string/title_activity_personal_infromation"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HistoryOrderActivity"
            android:label="@string/title_activity_history_order"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AboutTianduanActivity"
            android:label="@string/title_activity_about_tianduan"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HelpActivity"
            android:label="@string/title_activity_help"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SettingActivity"
            android:label="@string/title_activity_setting"
            android:theme="@style/AppTheme" />
        <activity android:name=".UnSignedOrderDetailActivity" />
        <activity android:name=".UnchekinOrderDetailActivity" />
        <activity android:name=".UnfinishedOrderDetailActivity" />
        <activity android:name=".UncommentOrderDetailActivity" />
        <activity android:name=".FinishedOrderDetailActivity"></activity>
    </application>

</manifest>